// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql

package searchrepo

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listPageTitlesForIndex = `-- name: ListPageTitlesForIndex :many
SELECT id, title
FROM docs.pages
WHERE deleted_at IS NULL
`

type ListPageTitlesForIndexRow struct {
	ID    pgtype.UUID
	Title string
}

// Every live page's id/title — internal/search's own BK-tree rebuild
// reads this on its refresh cadence (NOT on every request: the whole
// point of a rebuilt-periodically index, search.html's own admitted
// "may lag the write path").
func (q *Queries) ListPageTitlesForIndex(ctx context.Context) ([]ListPageTitlesForIndexRow, error) {
	rows, err := q.db.Query(ctx, listPageTitlesForIndex)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListPageTitlesForIndexRow
	for rows.Next() {
		var i ListPageTitlesForIndexRow
		if err := rows.Scan(&i.ID, &i.Title); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchBlockText = `-- name: SearchBlockText :many
SELECT
    b.page_id AS page_id,
    p.title AS page_title,
    b.id AS block_id,
    ts_headline('english', coalesce(b.content->>'text', ''), websearch_to_tsquery('english', $1::text)) AS snippet,
    ts_rank(b.search_vector, websearch_to_tsquery('english', $1::text)) AS rank
FROM docs.blocks b
JOIN docs.pages p ON p.id = b.page_id
WHERE p.deleted_at IS NULL
  AND b.search_vector @@ websearch_to_tsquery('english', $1::text)
ORDER BY rank DESC
LIMIT 20
`

type SearchBlockTextRow struct {
	PageID    pgtype.UUID
	PageTitle string
	BlockID   pgtype.UUID
	Snippet   []byte
	Rank      float32
}

// A block-text hit, joined back to its page for the title a result card
// needs. ts_headline builds the <b>...</b>-wrapped snippet directly —
// search.html's own "the snippet shows why it matched," computed once
// here rather than re-deriving match spans client-side.
func (q *Queries) SearchBlockText(ctx context.Context, query string) ([]SearchBlockTextRow, error) {
	rows, err := q.db.Query(ctx, searchBlockText, query)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SearchBlockTextRow
	for rows.Next() {
		var i SearchBlockTextRow
		if err := rows.Scan(
			&i.PageID,
			&i.PageTitle,
			&i.BlockID,
			&i.Snippet,
			&i.Rank,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchPageTitles = `-- name: SearchPageTitles :many
SELECT id, title, ts_rank(search_vector, websearch_to_tsquery('english', $1::text)) AS rank
FROM docs.pages
WHERE deleted_at IS NULL
  AND search_vector @@ websearch_to_tsquery('english', $1::text)
ORDER BY rank DESC
LIMIT 20
`

type SearchPageTitlesRow struct {
	ID    pgtype.UUID
	Title string
	Rank  float32
}

// A title-only hit — websearch_to_tsquery understands quoted phrases
// and bare-word OR/AND the way a user actually types a query box, unlike
// plainto_tsquery (AND-only) or the raw to_tsquery operator syntax
// (search.html is a plain <input>, not a query language). Ranked by
// ts_rank against docs.pages' own GENERATED ALWAYS tsvector column
// (migration 00004) — transactionally consistent with title, never
// stale the way the BK-tree title index deliberately can be.
func (q *Queries) SearchPageTitles(ctx context.Context, query string) ([]SearchPageTitlesRow, error) {
	rows, err := q.db.Query(ctx, searchPageTitles, query)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SearchPageTitlesRow
	for rows.Next() {
		var i SearchPageTitlesRow
		if err := rows.Scan(&i.ID, &i.Title, &i.Rank); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
